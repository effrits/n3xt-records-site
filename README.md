# N3XT Records site

Static site ready for Vercel.

- Open `index.html` locally to test.
- Deploy the whole folder to Vercel.
- Media assets are stored under `assets/` instead of being embedded in the HTML.
